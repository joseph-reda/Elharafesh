#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# ضغط الصور ورفعها لو ممكن، بدون كسر commit
npm run compress-and-upload || echo "⚠️ لم يتم رفع الصور، لكن يمكنك متابعة commit"

# إضافة الملفات المضغوطة تلقائيًا
git add public/images/compressed
